import { MobileExecutionView } from "@/components/mobile/execution/MobileExecutionView";

export const dynamic = "force-dynamic";
export const metadata = { title: "Execution — Capitalife Terminal" };

export default function MobileExecutionPage() {
  return <MobileExecutionView />;
}
